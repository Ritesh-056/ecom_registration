import 'package:flutter/material.dart';

const Color kPrimaryColor = Colors.green;
const Color iconColor = Colors.white;



const String textString = 'In some situations, you want to update the display of an app when the user rotates the screen from portrait mode to landscape mode. For example, the app might show one item after the next in portrait mode, yet put those same items side-by-side in landscape mode.';